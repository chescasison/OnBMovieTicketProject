/**
 * The domain model. This is the heart of the application, made up of
 * aggregates, entities, value objects, along with the repository interface,
 * factories and exceptions where applicable.
 * <p>
 * For aggregates that have several child entities and value objects, move them
 * to their own sub-package. And take advantage of package private (default)
 * access level (visibility).
 */
package movietickets.domain;
